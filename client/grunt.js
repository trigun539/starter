module.exports = function(grunt){
	grunt.initConfig({

		// TASKS LIST

	});

	// Watch 
	// watch: {
	// 	sass: {
	// 		files: ['app/sasss/*'], // Files to watch
	// 		tasks: 'sass' // Task to execute whe files are changed
	// 	}
	// }

	// Load NPM Tasks
	// grunt.loadNpmTasks('grunt-contrib-sass');
};